import { Git, Github, Vercel, CloudFlare, AWS } from "@/components/icons/techskills/infra";

const infraTechSkillMap = {
  Git: Git,
  Github: Github,
  Vercel: Vercel,
  CloudFlare: CloudFlare,
  AWS: AWS,
} as const;

export default infraTechSkillMap;
